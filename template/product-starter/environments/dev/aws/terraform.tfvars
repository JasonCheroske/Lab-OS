# Committed non-secret defaults for LocalStack / learning paths only.
# Replace with secure values and remote state before any real AWS environment.

use_localstack = true
db_password    = "lab_local_only_change_me"
