node_modules/
.tmp/

# Terraform — never commit provider caches (templates may run terraform init locally)
**/.terraform/
coverage/
dist/
*.log
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.tmp

# Personal harness config (API keys, session state, individual preferences)
# .ai/ is committed; personal config at the repo root is not.
.cursor/settings.json
.cursor/chat/
.claude/
